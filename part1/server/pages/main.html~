<!DOCTYPE html>
<html lang="pl" ng-app="pizzaPortal">
<head>
  <head>
    <meta charset="utf-8">
    <title>Pizzeria Italiano</title>
    <link rel="stylesheet" type="text/css" href="/style.css">
    <script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.2.26/angular.min.js"></script>
    <script type="text/javascript" src="/angular-ui-router.js"></script>
    <script src="/app.js"></script>
    <script src="/mainCtrl.js"></script>
    <script src="/orderCtrl.js"></script>
    <script src="/statusCtrl.js"></script>
    <script src="/contactCtrl.js"></script>
 </head>
  <body ng-controller="mainCtrl">
	  <div class="menu">
                <span class="text">Menu</span>
          <div class="menu" data-ng-init="getMenu()"> 
            <table id="menu">
		<div>
                <tr>
                    <th>Nr</th>
                    <th>Pizza</th>
                    <th>Składniki</th>
                    <th>Cena</th>
                    <th>Ilość</th>
                    <th></th>
                </tr>

                <tr ng-repeat="itemMenu in menuResponse">
                    <td id="itemMenuId">  {{ itemMenu.id }} </td>
                    <td> {{ itemMenu.name}} </td>
                    <td>{{itemMenu.ingredients.toString()}}</td>
                    <td> {{ itemMenu.price.toFixed(2)}} </td>
                    <td>
			<input type="number" min="1" max="100" ng-init="itemMenu.quantity=1" ng-model="itemMenu.quantity" >
		    </td>
                    <td id="addButton">
			<img id="addButton" ng-src="{{addButton}}" class="add" ng-click="addToBasket(itemMenu)" />
                    </td>
                </tr>
		</div>
            </table>
	 </div>

          <div class="basket">
                <span class="text">Koszyk</span>
		<table id="basket">
                <tr>
                    <th>Pizza</th>
                    <th>Ilość</th>
                    <th>Cena</th>
                    <th></th>
                </tr>
                <tr ng-repeat="order in orders">
                    <td>{{order.name}}</td>   
                    <td>{{order.quantity}}</td>   
                    <td>{{order.price.toFixed(2)}}</td>   
                    <td>
			<img id="deleteButton" ng-src="{{deleteButton}}" class="delete" ng-click="deleteFromBasket(order.getId())" />
		  </td>
		</tr>
		</table>
        	<tr>
                <span class="text">Suma: {{sumPrice()}} </span>
		<div>
		<img id="orderButton" ng-src="{{orderButton}}" class="order" ng-click="placeOrder()" />
		</div>
          	</tr>
  	</div> 
      </div>          
 </body>
</html>
