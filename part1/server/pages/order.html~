<!DOCTYPE html>
<html lang="pl" ng-app="pizzaPortal">
<head>
  <head>
    <meta charset="utf-8">
    <title>Zamówienia</title>
    <link rel="stylesheet" type="text/css" href="/style.css">
    <script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.2.26/angular.min.js"></script>
    <script src="https://code.angularjs.org/1.4.8/angular-messages.min.js"></script>
    <script src="app.js"></script>
    <script src="/mainCtrl.js"></script>
    <script src="/orderCtrl.js"></script>
    <script src="/statusCtrl.js"></script>
    <script src="/contactCtrl.js"></script>
  </head>
    
  <body ng-controller="orderCtrl">
  <h1>Zamówienie numer  <div data-ng-init="getOrder()">
            {{orderResponse.name}}
    </div></h1>
    <div class="orderSummary">

        <table id="summary">
                    <tr>
                        <th>Nazwa</th>
                        <th>Ilość</th>
                        <th>Cena</th>
                 
                    </tr>
                    <tr ng-repeat="order in orders">
                        <td>{{order.name}}</td>   
                        <td>{{order.quantity}}</td>   
                        <td>{{order.price.toFixed(2)}}</td>   
                    </tr>
                    <tr>
                        <td><b>Razem do zapłaty: {{sumPrice()}}</b></td>
                        <td></td>
                    </tr>  
                    <tr>
                    </tr>
                    </table>

    </div>
    
    <div class="orderForm">  
        <p>Wypełnij dane potrzebne do zamówienia</p>
        <form name="myForm" action="status/:orderId">
          <div ng-class="{error: isInvalid('phoneNumber')}">
                <label>
                    Numer telefonu: 
                </label>
                <input type="text" name="phoneNumber" placeholder="Podaj nr telefonu" ng-model="phoneNumber" required/>
                <span ng-show="isInvalid('phoneNumber')">Numer telefony jest wymagany</span>

        </div>
               <div ng-class="{error: isInvalid('street')}">
                <label>
                    Ulica:
                </label>
                <input type="text" placeholder="Podaj adres" name="street" ng-model="street" required>
                <span ng-show="isInvalid('street')">Dodanie adresu jest wymagane</span>
           </div>

            <div>
                <label>
                    Uwagi: 
                </label>
                <input class="order" placeholder="Dodatkowe informacje" type="text" name="remarks">
            
            <div class="orderBtn">
                <input type="button" value="Powrót" ng-click="goBack()">
                <button ng-disabled="myForm.$invalid">Zatwierdź</button>
             </div> 
            </div>
        </form>
    
    
    </div>
      
    
      
</body>
</html>
